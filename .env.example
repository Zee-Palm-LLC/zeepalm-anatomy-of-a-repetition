# The public origin of the deployed site. Canonical URLs, the sitemap and the
# Open Graph card are absolute, so this must be set before the first deploy.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
