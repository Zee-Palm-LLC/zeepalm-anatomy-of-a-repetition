/** Shared surface treatment, so every card on the site reads as one system. */
export const CARD =
  "rounded-2xl border border-white/[0.07] bg-[#0F1724] shadow-[0_1px_2px_rgba(0,0,0,0.3),0_18px_44px_-24px_rgba(0,0,0,0.7)]";

/**
 * Two colours, two jobs — and they must never be confused for each other.
 *
 * BRAND is Zee Palm's own accent, lifted from zeepalm.com. It carries the
 * company and the interface: navigation, buttons, links.
 *
 * DATA is the top of the activation ramp. It carries effort, and nothing else.
 * Keeping the brand out of the data palette is the same rule that governs the
 * muscle-group accents: if a colour encodes a value, it cannot also be
 * decoration.
 */
export const BRAND = "#21B083";
export const BRAND_LIT = "#4FD1A5";
export const DATA = "#FF5E3F";
